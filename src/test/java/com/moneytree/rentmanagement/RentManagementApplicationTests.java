package com.moneytree.rentmanagement;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class RentManagementApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the full Spring application context starts successfully
        // (against the in-memory H2 datasource configured in src/test/resources).
    }
}
