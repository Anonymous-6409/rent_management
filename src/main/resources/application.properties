# ===============================
# Server
# ===============================
server.port=8080

# ===============================
# MySQL DataSource
# ===============================
# Create the database first:  CREATE DATABASE rent_management;
spring.datasource.url=jdbc:mysql://localhost:3306/rent_management?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===============================
# Thymeleaf
# ===============================
spring.thymeleaf.cache=false

# ===============================
# Payment Reminders
# ===============================
# Day of the month rent is considered due (used to compute the due date).
app.reminders.due-day=5
# Cron for the scheduled job that generates + sends reminders (default: daily 09:00).
app.reminders.cron=0 0 9 * * *
# When true, reminders are emailed via SMTP. When false they are only persisted + logged.
app.reminders.email.enabled=true
# Optional "from" address used on reminder emails.
app.reminders.email.from=no-reply@rentmanager.local

# ===============================
# Mail (only needed when app.reminders.email.enabled=true)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=arjunsingh.as7670400@gmail.com
spring.mail.password=xmkrctlfjviouelv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===============================
# Security — default seeded admin account
# Created on first start-up if it does not already exist. Change after first login.
# ===============================
app.security.admin.username=admin
app.security.admin.password=admin123
